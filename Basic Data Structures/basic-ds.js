// Below is an example of the simplest implementation of an array data structure.
let simpleArray = ['one', 2, 'three', true, false, undefined, null];
console.log(simpleArray.length);

/* This is known as a one-dimensional array, meaning it only has one level, 
 * or that it doesn't have any other arrays nested within it. Notice it contains
 * booleans, strings, and numbers, among other valid JavaScript data types.
 * 
 * We defined a variable called yourArray. Complete the statement by assigning an array of at
 * least 5 elements in length to the yourArray variable. Your array should contain at least one string,
 * one number, and one boolean.
 */
let yourArray = ['hello', 23, false, 'world', true];